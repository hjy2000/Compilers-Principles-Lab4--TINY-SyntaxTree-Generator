{ Sample program
  in TINY language -
  computes factorial
}
read x; { input an integer }
if 0 <= x then { don't compute if x <= 0 }
  fact = 1;
  for i = 0 to 2
  do 
    fact = fact ^ x;
    x = x - 1;
    write fact;  { output factorial of x}
enddo
end